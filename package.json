{
  "name": "json-server-books",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch my_data.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "0.17.4"
  }
}
